export type User = {
    id: number
    company_name: string,
    github_id: null,
    github_login: string,
    github_avatar: string
    github_token: string,
    email: string
    created_at: string
}